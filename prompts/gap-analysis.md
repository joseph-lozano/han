---
description: Compare current and desired states and produce an indexed gap report.
argument-hint: "<current state> <desired state> [options]"
---
Use the `gap-analysis` skill. Treat the following as the skill arguments and user context:

$ARGUMENTS
