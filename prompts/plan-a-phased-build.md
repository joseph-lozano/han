---
description: Split a body of work into demoable vertical-slice phases.
argument-hint: "<context path or description>"
---
Use the `plan-a-phased-build` skill. Treat the following as the skill arguments and user context:

$ARGUMENTS
