---
description: Turn a feature specification into an implementation plan.
argument-hint: "[size] <feature specification path>"
---
Use the `plan-implementation` skill. Treat the following as the skill arguments and user context:

$ARGUMENTS
